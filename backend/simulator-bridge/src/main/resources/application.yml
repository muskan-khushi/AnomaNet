server:
  port: 8087

spring:
  application:
    name: simulator-bridge

services:
  ml-service: ${ML_SERVICE_URL:http://localhost:8000}

springdoc:
  swagger-ui:
    path: /swagger-ui

logging:
  level:
    com.anomanet: INFO
